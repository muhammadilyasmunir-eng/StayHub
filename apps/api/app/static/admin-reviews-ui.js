(()=>{
const token=()=>localStorage.getItem('stayhub_admin_token')||'';const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const api=async(url,opt={})=>{const r=await fetch(url,{...opt,headers:{Authorization:`Bearer ${token()}`,'Content-Type':'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.detail||'Request failed');return d};
const f=[['overall_score','Overall'],['staff_score','Staff'],['facilities_score','Facilities'],['cleanliness_score','Cleanliness'],['comfort_score','Comfort'],['value_score','Value'],['location_score','Location'],['wifi_score','WiFi']];
async function load(){const sec=document.getElementById('reviews');if(!sec||sec.classList.contains('hidden'))return;const rows=await api('/admin/reviews').catch(()=>[]);let box=sec.querySelector('.arw');if(!box){box=document.createElement('div');box.className='arw';sec.querySelector('.card')?.replaceChildren(box)}box.innerHTML=rows.length?rows.map(r=>`<article class="ar-card"><div class="ar-top"><div><b>${esc(r.guest_name)}</b><span>${esc(r.hotel_name)} · #${esc(r.confirmation_no)}</span></div><strong>${Number(r.overall_score).toFixed(1)}/10</strong></div><h3>${esc(r.title||'Guest review')}</h3><p>${esc(r.comment)}</p><div class="ar-cats">${f.slice(1).map(x=>`${x[1]} ${Number(r[x[0]]).toFixed(1)}`).join(' · ')}</div><div class="ar-actions"><button data-edit="${r.id">Edit</button><button class="danger" data-del="${r.id}">Delete</button>${r.deleted?'<em>Deleted</em>':''}</div></article>`).join(''):'<div class="empty">No reviews found.</div>';box.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>edit(b.dataset.edit,rows.find(x=>String(x.id)===b.dataset.edit)));box.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>del(b.dataset.del))}
async function edit(id,r){if(!r)return;const values={};for(const [k,l] of f){const v=prompt(`${l} score (1-10):`,r[k]);if(v===null)return;values[k]=Number(v)}const title=prompt('Review title:',r.title||'');if(title===null)return;const comment=prompt('Review text:',r.comment||'');if(comment===null)return;values.title=title;values.comment=comment;values.admin_note=prompt('Admin note (optional):',r.admin_note||'')||null;try{await api(`/admin/reviews/${id}`,{method:'PUT',body:JSON.stringify(values)});load()}catch(e){alert(e.message)}}async function del(id){if(!confirm('Delete this review from StayHub public listings?'))return;try{await api(`/admin/reviews/${id}`,{method:'DELETE'});load()}catch(e){alert(e.message)}}
const style=document.createElement('style');style.textContent=`.arw{display:grid;gap:14px}.ar-card{border:1px solid #e1e6ee;border-radius:14px;padding:18px;background:#fff}.ar-top{display:flex;justify-content:space-between}.ar-top span{display:block;color:#667085;font-size:11px;margin-top:4px}.ar-top strong{font-size:21px;color:#1d4ed8}.ar-card h3{margin:12px 0 5px}.ar-card p{color:#475467}.ar-cats{font-size:11px;color:#667085;padding:10px;background:#f8fafc;border-radius:8px}.ar-actions{display:flex;gap:8px;margin-top:12px}.ar-actions button{border:1px solid #d0d5dd;background:#fff;border-radius:8px;padding:8px 12px;font-weight:800;cursor:pointer}.ar-actions .danger{color:#b91c1c;border-color:#f1c5c8}`;document.head.appendChild(style);setInterval(load,3000);load();
})();
