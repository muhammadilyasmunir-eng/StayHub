(() => {
  const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
  const money=v=>Number(v||0).toLocaleString('en-PK',{minimumFractionDigits:2,maximumFractionDigits:2});
  async function load(){
    const target=document.querySelector('#finance, [data-section="finance"], #adminFinance'); if(!target)return;
    try{const r=await fetch('/finance/admin');const d=await r.json();if(!r.ok)throw Error(d.detail||'Finance unavailable');target.innerHTML=`<div class="section-heading"><div><span class="eyebrow">PLATFORM FINANCE</span><h2>Reservation Commission</h2><p>StayHub platform commission and owner settlement view.</p></div></div><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin-bottom:18px"><div class="card"><small>Total Sales</small><h2>PKR ${money(d.total_sales)}</h2></div><div class="card"><small>Total Commission</small><h2>PKR ${money(d.total_commission)}</h2></div><div class="card"><small>Owner Earnings</small><h2>PKR ${money(d.total_owner_earnings)}</h2></div></div><div class="card" style="overflow:auto"><table class="table"><thead><tr><th>Property</th><th>Booking</th><th>Status</th><th>Total</th><th>Commission</th><th>Owner</th></tr></thead><tbody>${d.reservations.map(x=>`<tr><td>${esc(x.hotel_name||x.hotel_id)}</td><td><b>${esc(x.confirmation_no)}</b><br><small>${esc(x.booking_source)}</small></td><td>${esc(x.status)} / ${esc(x.commission_status)}</td><td>PKR ${money(x.total_amount)}</td><td>PKR ${money(x.commission_amount)} (${money(x.commission_percent)}%)</td><td>PKR ${money(x.owner_amount)}</td></tr>`).join('')||'<tr><td colspan="6">No reservation finance records.</td></tr>'}</tbody></table></div>`}catch(e){target.innerHTML=`<div class="empty">Finance unavailable: ${esc(e.message)}</div>`}}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(load,800));else setTimeout(load,800);
})();
